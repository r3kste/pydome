from setuptools import setup, find_packages

setup(
    name="pydome",
    version="0.1.1",
    author='Anton "r3kste" Beny',
    packages=find_packages(),
    install_requires=["numpy", "scipy", "optimagic"],
)
